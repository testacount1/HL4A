var _底层 = 线性布局(当前界面)
.置主题("底层")
.打开(当前界面);

var _主页 = 线性布局(当前界面)
.置主题("底层")
.置高度("最大")
.加入到(_底层);

var _标题栏 = 标题栏(当前界面)
.置标题("你好 世界！")
.左按钮("返回",function() {
    当前界面.结束();
})
.加入到(_主页);

var _内容 = 线性布局(当前界面)
.置主题("底层")
.置高度("最大")
.置填充("16dp")
.加入到(_主页);

var _文本 = 文本控件(当前界面)
.置文本("Hello HL4A！")
.加入到(_内容);