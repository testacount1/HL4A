apply plugin: 'java'

dependencies {
    testCompile fileTree(dir: 'libs', include: ['junit*.jar', 'hamcrest*.jar'])
}

sourceSets {
    test {
        java {
            srcDirs = ["src/test/java"]
        }
    }
}